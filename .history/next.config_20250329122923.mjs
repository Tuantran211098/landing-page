const nextConfig = {
    images: {
        remotePatterns: [
            {
                protocol: "https",
                hostname: "beta-api.bachlongmobile.com",
            },
        ],
    },
};

export default nextConfig;
